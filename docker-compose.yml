version: '3.8'

services:
  lite:
    build: .
    container_name: dingpang-lite
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

networks:
  lite-network:
    driver: bridge
